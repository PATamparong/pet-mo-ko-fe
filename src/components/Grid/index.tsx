import React from "react";
import * as S from "./styles";
import { GridPropTypes } from "../../types/Props";

export default function Grid(props: GridPropTypes<undefined>) {
  return <S.Container></S.Container>;
}
